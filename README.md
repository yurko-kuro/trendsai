# TrendsAI

Програмний комплекс для прогнозування динаміки пошукових запитів Google Trends
із використанням гібридної нейромережевої моделі Transformer–GRU.

Проєкт розроблено в межах дипломної роботи
за спеціальністю 124 «Системний аналіз».

---

## 📌 Основні можливості
- автоматизований збір даних Google Trends;
- збереження історичних даних у SQLite;
- прогнозування часових рядів за допомогою моделі Transformer–GRU;
- формування звітів (CSV, PNG, JSON);
- семантичний відбір та ранжування ключових слів (Sentence-BERT);
- повністю керований CLI-конвеєр.

---

## 🧱 Структура проєкту
```
TrendsAI/
├── manage.py              # CLI-інтерфейс
├── requirements.txt       # залежності
├── db.sqlite3             # SQLite база даних
├── modules/
│   ├── core/              # збір даних, підготовка, моделі
│   ├── alt/               # семантичний підбір альтернатив
│   └── report/            # звіти та метрики
└── reports/               # результати експериментів
```

---

## ⚙️ Встановлення

```bash
python -m venv .venv
source .venv/bin/activate      # Linux / macOS
.\.venv\Scripts\activate    # Windows
pip install -r requirements.txt
```

---

## ▶️ Основні команди

### Збір даних
```bash
python manage.py collect куртка
python manage.py collect "жіноча куртка" "чоловіча куртка"
```

### Побудова прогнозу та звіту
```bash
python manage.py report куртка -p 30 -s 12
```

### Швидкий перегляд прогнозу
```bash
python manage.py view куртка -p 30 -s 12
```

### Семантичний підбір альтернатив
```bash
python manage.py suggest куртка \
  --top 3 \
  --collect-missing \
  --with-metrics \
  --metric rmse \
  --save
```

---

## 📊 Метрики
- RMSE
- MAE
- R²
- MAPE
- sMAPE

Метрики зберігаються у файлах `*_metrics.json`.

---

## 📚 Контекст
Проєкт використовується для експериментального дослідження
ефективності гібридної моделі Transformer–GRU
у задачах SEO-прогнозування.
